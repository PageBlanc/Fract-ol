services:
  fract-ol:
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ~/.Xauthority:/root/.Xauthority:rw
      - .:/Fract-ol
    network_mode: host
    stdin_open: true
    tty: true
